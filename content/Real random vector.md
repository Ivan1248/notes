A **real random vector** is a [[Random variable|random variable]] $\rvec x\colon \Omega\to \R^n$, where $n\in \N$.  

## Notation

- $\rvec x[i]$ (or $\rvec x_{[i]}$) denotes the random variable $\omega \mapsto \rvec x(\omega)[i] \colon \Omega\to \R$.

## Related

- [[Real vector]]
