Let $f$ be a [[Real function|real function]].  
Let $S$ be the set in which $f$ is [[Differentiable function|differentiable]].

The **derivative** of $f$ is a [[Real function|real function]] $f'$ with domain $S$ such that
$$
\forall x\in S\colon f'(x) = \lim_{\h \to 0}\frac{f(x+h)-f(x)}{h} .
$$
