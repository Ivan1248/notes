---
aliases:
  - Argmin
---
## Argmax

$$
\argmax_{x:P(x)} f(x) = \{x: f(x) = f(\max_{x\in X} f(x))\}
$$

## Argmin

$$
\argmax_{x:P(x)} f(x) = \{x: f(x) = f(\max_{x\in X} f(x))\}
$$

## Arg

$$
\arg^S_{x:P(x)} f(x) = \{x : f(x) = f(S(\{f(x) : x\in X\})\}
$$
